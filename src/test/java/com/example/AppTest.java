package com.example;

import org.junit.Test;
import static org.junit.Assert.assertTrue;

public class AppTest {
    @Test
    public void testApp() {
        // This is a simple test case to ensure the main method runs without errors
        App.main(new String[]{});
        assertTrue(true);
    }
}
